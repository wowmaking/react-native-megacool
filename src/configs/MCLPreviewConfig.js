export default class MLCPreviewConfig {
  setRecordingId(recordingId) {
    this.recordingId = recordingId;
    return this;
  }
  setPreviewFrame(previewFrame) {
    this.previewFrame = previewFrame;
    return this;
  }
}